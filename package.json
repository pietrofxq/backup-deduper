{
  "name": "safe-dedupe",
  "version": "0.1.0",
  "description": "A safety-first deduplication tool for collections of files in a directory tree.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('node:fs').cpSync('src/db/migrations','dist/db/migrations',{recursive:true})\"",
    "build:web": "vite build --config web/vite.config.ts",
    "start": "node --enable-source-maps dist/main.js",
    "dev": "tsx watch src/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:thorough": "FAST_CHECK_NUM_RUNS=500 vitest run tests/integration/safetyInvariant.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no linter configured)\""
  },
  "dependencies": {
    "@fastify/static": "^7.0.0",
    "better-sqlite3": "^11.5.0",
    "drizzle-orm": "^0.45.2",
    "fast-glob": "^3.3.3",
    "fastify": "^5.0.0",
    "fastify-type-provider-zod": "^6.1.0",
    "pino": "^9.5.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.16.0",
    "drizzle-kit": "^0.31.10",
    "fast-check": "^3.22.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
